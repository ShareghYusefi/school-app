import { ColorfulBgDirective } from './colorful-bg.directive';

describe('ColorfulBgDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorfulBgDirective();
    expect(directive).toBeTruthy();
  });
});
